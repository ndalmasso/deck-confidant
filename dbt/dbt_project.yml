name: 'deck_confidant'
version: '1.0.0'
config-version: 2

profile: 'deckconfidant'

model-paths: ["models"]
test-paths: ["tests"]
log-path: "logs"

models:
  deck_confidant:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
